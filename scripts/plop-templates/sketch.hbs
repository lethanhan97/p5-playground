
import P5 from "p5";
import { CANVAS_SIZE } from "./constants";
import { BaseSketch } from "./types";

interface Colors {
  // TODO: Add colors
}

export default class {{pascalCase name}} extends BaseSketch {
  _colors: Colors;

  constructor({ p5 }: { p5: P5 }) {
    super({ p5, name: "{{pascalCase name}}" });

    this._colors = {
      // TODO: Add colors
    };
  }

  setup = () => {
    this._p5.createCanvas(CANVAS_SIZE, CANVAS_SIZE);
  };

  draw = () => {};
}
